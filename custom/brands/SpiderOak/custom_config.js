/* Brand-specific custom configuration settings, overrides defaults.
 *
 * You only need to specify those settings from config.js that you wish to
 * override.
 */

window.spiderOakMobile_custom_config = {
  inhibitAdvancedLogin: {value: false, retain: 0}
};
